# Notion page — `Checkout Reliability`

A container page. Fictional; create it by hand in your own Notion workspace. Lore
never writes to Notion.

| Field | Value |
| --- | --- |
| Title | `Checkout Reliability` |
| Parent | **inside the `Subsystems` toggle** on [`Engineering Decisions`](notion-engineering-decisions.md) |
| Created | 2024-02-11 in the fixtures; any date before the incident is fine |

## Body

Empty. Its only job is to hold the decision page one level deeper, so the scope
walk has to climb page → block → page to reach the root.

## Setup

Open `Engineering Decisions`, expand the `Subsystems` toggle, and create a
sub-page inside it named `Checkout Reliability`. Do not share it separately: it
inherits the integration's access from the root.

## What lore stores

| Field | Value |
| --- | --- |
| Document id | `notion:page:<dashed page id>` |
| Title | `Checkout Reliability` |
| Author | *empty* |
| URL | the page's canonical `notion.so` URL |

It has no body, so it matches no query and links to nothing. It appears in
`lore status` counts and nowhere else in the walkthrough.
